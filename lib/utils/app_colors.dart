import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF262450);
  static const navIConColor = Color(0xFF00D7FF);
  static const navIConUnSelectedColor = Color(0xFF7B78AA);
  static const bgColor = Color(0xFF19173D);
  static const navBgColor = Color.fromRGBO(255, 255, 255, 0.30);
  static const white = Colors.white;
  static const black = Colors.black;
  static const grey = Colors.grey;
  static const lightGrey = Colors.grey;
}
